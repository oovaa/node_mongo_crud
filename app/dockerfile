FROM oven/bun:latest

WORKDIR /app

# Copy entire app into the container (needed for the build step)
COPY . /

# Install dependencies and build the Vite app
RUN bun install
RUN bun run build

EXPOSE 5463

# Vite preview - expose to network (use --host if you need access from other hosts)
CMD ["bun", "preview", "--host"]